# v0.1 Release Governance

## Goal

Close the Phase 1.5 release-governance slice for the v0.1 MVP demo milestone:
document the real branch-protection state, prepare release notes, and publish a
traceable `v0.1.0` tag once the release-hygiene commit passes CI.

## Scope

- Do not change product runtime behavior.
- Keep the release honest: v0.1 is a functional local MVP, not a production
  launch.
- Treat GitHub branch protection as an external repository capability.
- Use the existing CI jobs as the release verification source:
  - `lint`
  - `unit`
  - `e2e-smoke`
  - `postgres-integration`
  - `release-readiness`

## Plan

- [x] Confirm the repository permission, visibility, and default branch.
- [x] Check current GitHub branch-protection state for `main`.
- [x] Record the branch-protection blocker in docs.
- [x] Convert `CHANGELOG.md` from `Unreleased`-only to a `v0.1.0` release entry.
- [x] Run local release-hygiene checks.
- [x] Commit and push release-governance documentation.
- [x] Wait for CI on the release-governance commit.
- [x] Create and push the annotated `v0.1.0` tag.
- [x] Create a GitHub release for the demo milestone if tag publication succeeds.

## Discoveries

- Discovery: The repo is private and the current viewer has admin access.
  Evidence: `gh repo view --json nameWithOwner,viewerPermission,defaultBranchRef,isPrivate`
  returned `viewerPermission=ADMIN`, `isPrivate=true`, and `defaultBranchRef.name=main`.
- Discovery: GitHub branch protection cannot currently be enabled for this
  private repository.
  Evidence: `gh api repos/clearlove1fan/autonomous-ads-growth-agent-platform/branches/main/protection`
  returned HTTP 403 with `Upgrade to GitHub Pro or make this repository public to enable this feature.`
- Discovery: The functional MVP verification baseline has passing CI.
  Evidence: CI run `26022065806` for commit `53806c0` completed successfully
  across `lint`, `unit`, `e2e-smoke`, `postgres-integration`, and
  `release-readiness`.
- Discovery: The release-governance commit has passing CI.
  Evidence: CI run `26022460339` for commit `085fd7e` completed successfully
  across `lint`, `unit`, `e2e-smoke`, `postgres-integration`, and
  `release-readiness`.

## Decisions

- Decision: Do not claim branch protection is implemented.
  Reason: GitHub plan limits currently block the repository setting.
- Decision: Continue with a `v0.1.0` demo tag after release-hygiene CI passes.
  Reason: The functional MVP and CI gates are ready, and the branch-protection
  gap is documented as an external blocker.

## Verification

- [x] `git diff --check`
  Result: Passed.
- [x] `.venv/bin/ruff check .`
  Result: Passed.
- [x] `.venv/bin/pytest`
  Result: 174 passed, 18 skipped.
- [x] `.venv/bin/ads-growth-agent --version`
  Result: `0.1.0`.

## Final Status

Completed. The `v0.1.0` annotated tag points to commit `085fd7e`, and the
GitHub Release is published at
`https://github.com/clearlove1fan/autonomous-ads-growth-agent-platform/releases/tag/v0.1.0`.
Branch protection remains documented but externally blocked for this private
repository until GitHub Pro is enabled or the repository is made public.
